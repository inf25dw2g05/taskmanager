# Capítulo 2 — Arquitectura y modelo de datos

## 2.1. Arquitectura general

## 2.1. Arquitectura general

La aplicación sigue una arquitectura cliente-servidor dividida en tres componentes principales: una aplicación frontend desarrollada con ReactJS, una API REST desarrollada con Node.js y Express, y una base de datos MySQL para el almacenamiento persistente de la información.

La comunicación entre los distintos componentes se realiza mediante peticiones HTTP. El frontend consume los endpoints expuestos por la API REST, mientras que la API interactúa con la base de datos para almacenar y recuperar la información solicitada.

La arquitectura general del sistema puede representarse de la siguiente forma:

Frontend ReactJS
↓
API REST (Node.js + Express)
↓
MySQL

## 2.2. Estructura de la aplicación

La solución está dividida en dos bloques principales: frontend y backend.

El frontend se encuentra en el directorio `frontend/` y contiene la aplicación React encargada de la interacción con el usuario.

Los principales componentes implementados son:

- `Login.js` — Autenticación de usuarios.
- `Profile.js` — Consulta del perfil del usuario autenticado.
- `Projects.js` — Gestión de proyectos.
- `Tasks.js` — Gestión de tareas.
- `App.js` — Componente principal y navegación entre vistas.

El backend se encuentra en el directorio `src/` y mantiene la estructura desarrollada durante M1 basada en rutas, controladores y middlewares.

## 2.3. Flujo de funcionamiento

El funcionamiento de la aplicación comienza cuando el usuario introduce sus credenciales en el formulario de login.

La aplicación React realiza una petición al endpoint `/auth/login`. Si las credenciales son válidas, la API devuelve un token JWT que se almacena en el navegador mediante `localStorage`.

A partir de ese momento, todas las peticiones protegidas incluyen la cabecera:

`Authorization: Bearer <token>`

La API valida el token antes de procesar cada solicitud y determina la identidad del usuario autenticado.

Una vez validado el usuario, la aplicación permite consultar y gestionar proyectos y tareas mediante operaciones CRUD.

## 2.4. Modelo de datos

El modelo está compuesto por **tres entidades** con **dos relaciones 1:n** entre ellas.

## 2.5. Datos de prueba

La base de datos incluye un conjunto de datos de prueba utilizados durante el desarrollo y la validación del sistema.

Entre estos datos se encuentran usuarios, proyectos y tareas previamente cargados en la base de datos, lo que permite verificar el funcionamiento de la API REST y de la aplicación React sin necesidad de introducir información manualmente tras cada despliegue.

Para facilitar las pruebas, se incluyen varios usuarios de demostración con credenciales conocidas:

| Email | Contraseña |
|---|---|
| `demo1@taskmanager.com` | `demo1234` |
| `demo2@taskmanager.com` | `demo1234` |
| `demo3@taskmanager.com` | `demo1234` |

Cada uno de estos usuarios dispone de proyectos y tareas asociados, lo que permite comprobar el correcto funcionamiento de las operaciones CRUD implementadas en la aplicación.

Además de los usuarios de demostración, la base de datos incorpora usuarios adicionales empleados para generar un conjunto de datos más amplio durante las pruebas. Todos ellos utilizan la misma contraseña:

| Usuarios | Contraseña |
|---|---|
| `alice`, `bob`, `carol`, `david`, `eva`, `frank`, `grace`, `helen`, `ivan`, `julia`, entre otros | `demo1234` |

Estos registros permiten verificar el aislamiento por propietario implementado en la aplicación, garantizando que cada usuario únicamente puede acceder a sus propios proyectos y tareas.

## 2.6. Despliegue con Docker Compose

La aplicación se distribuye mediante Docker Compose utilizando tres contenedores independientes.

- `taskmanager-mysql`
  - Servicio MySQL encargado del almacenamiento de datos.

- `taskmanager-app`
  - API REST desarrollada con Node.js y Express.

- `taskmanager-frontend`
  - Aplicación React encargada de la interfaz de usuario.

La comunicación entre los tres contenedores se realiza mediante una red Docker común denominada `taskmanager-net`.

Este enfoque permite desplegar la aplicación completa mediante un único comando:

```bash
docker compose up